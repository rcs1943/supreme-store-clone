export interface Product {
    id: number;
    title: string;
    description: string;
    imageUrl: string;
    dripPath: string;
}

export interface ProductGrid {
    category: string;
    dripPath: string;
    imageUrl: string;
}